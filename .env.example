# Environment template (repo root)

Prefer copying the app-specific examples:

- [`backend/.env.example`](backend/.env.example)
- [`frontend/.env.example`](frontend/.env.example)

Do not put secrets in this file. Local secrets live in `backend/.env` and `frontend/.env` (gitignored).
